$(function() {
  // $('.ui.sticky')
  //   .sticky({
  //     context: '#root'
  //   });

  $('.overlay').visibility({
    type: 'fixed',
    offset: 90
  });

  $('#museum-title').visibility({
    type: 'fixed',
  });
});
